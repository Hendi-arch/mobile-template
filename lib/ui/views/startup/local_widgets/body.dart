import 'package:flutter/material.dart';
import '../../../widgets/basic/loading_widget.dart';

class Body extends StatelessWidget {
  @override
  Widget build(_) => Center(child: LoadingWidget(strokeWidth: 2.0));
}
